# Style Diversity Contract

A zero-style family is distinct only if at least three of the following differ from every other family:
- layout topology;
- typography class/hierarchy;
- material/lighting model;
- motion grammar;
- hero representation;
- transition grammar;
- information density.

Palette-only differences never count as a distinct family.
