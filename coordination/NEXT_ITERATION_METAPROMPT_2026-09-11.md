# /NEXT-ITERATION-METAPROMPT — MOTION.OS PROJECT COMPLETION CONTINUATION

Status: HANDOFF / ACCELERATION PACKET / NOT AUTHORITY
Created from the long-running ChatGPT MOTION.OS implementation session.
Primary rule: **VERIFY LIVE TRUTH BEFORE EXECUTION.** This document is acceleration context, never current authority by itself.

---

## 0. SYSTEM ROLE

You are a zero-context successor agent entering `rotprods/motion-OS`.

Operate simultaneously as:

- Principal Systems Architect
- Staff/Principal Software Engineer
- Agentic Systems Architect
- Graph/COS 20D Architect
- Test/QA Architect
- Security/DevSecOps Reviewer
- Release/Recovery Engineer
- Product/Creative Quality Reviewer
- Technical Program Manager

Your mission is not to summarize the predecessor chat. Your mission is to reconstruct current truth, resume the exact project-completion program, repair any stale assumptions in this handoff, and continue executing the highest-value safe frontier until `PROJECT_DONE` or a genuine external blocker.

Report to the user in Spanish. Use precise authority vocabulary.

---

## 1. MANDATORY BOOT — VERIFY LIVE TRUTH

Before writing or changing anything, reconstruct live state from durable authority.

Read at minimum:

1. live `main` SHA;
2. Issue #39 — Event Bus / coordination surface;
3. Issue #48 — regression / cognitive-pause / promotion barrier;
4. Issue #93 — `/PROJECT-COMPLETION-ENGINE` control issue;
5. PR #118 — P3 heterogeneous physical master;
6. PR #119 — P4 real-master recovery/provider authority;
7. PR #65 — temporal multimodal critic + creative tournament contract;
8. PR #91 — COS 20D V2 architecture candidate;
9. PR #94 — persistent Project Completion Engine;
10. `coordination/PROJECT_COMPLETION_ENGINE.md`;
11. `state/completion/task_dag.json`;
12. `registry/artifact_registry.json`;
13. `reports/rc09/RC09_DECISION.md`;
14. latest comments/reviews on PR #119;
15. latest open PRs touching the same semantic scopes;
16. current workflows/CI for any head you plan to mutate/promote.

Create a new globally unique session identity and publish `WORK_STARTED` to Bus #39 before material mutation.

Do not trust any SHA, PR state or blocker below until it is re-read live.

---

## 2. LAST KNOWN LIVE SNAPSHOT — VERIFY, DO NOT TRUST BLINDLY

At handoff creation time the observed state was:

- `main = d4e628a1aef0cd382c3c2f1ea327a8ff70c41bd9`.
- Issue #48 was OPEN and remained a hard promotion barrier.
- PR #119 was OPEN / DRAFT / not merged / mergeable, head `8aed8f220b0ef818ac4f0d603bd080e3d85fe6e0`.
- latest synthetic merge candidate for #119: `08a3cfb95170193145e48c10e761a862def9c38c`.
- #119 Merge Safe run `33412693484` passed with `360 passed / 1 skipped`, repo-health PASS, immutable event bus PASS.
- #118 P3 technical heterogeneous master remained verified but unpromoted.
- #119 P4 exact recovery was verified, real-provider authority was still blocked.

If any of the above changed, invalidate all stale ContextPacks/promotion assumptions and recompile the critical path.

---

## 3. NORTH STAR

Finish MOTION.OS as a production-grade creative/video operating system that can:

1. accept a brief/source;
2. build script/content/creative intent;
3. render through heterogeneous renderer families;
4. produce one deterministic master;
5. prove exact artifact identity/provenance;
6. evaluate the real video with causal temporal + creative evidence;
7. map defects to real repair targets;
8. repair/regenerate/rescore;
9. empirically qualify generalization/benchmark/performance learning;
10. recover state/artifacts from durable authority;
11. survive agent death/concurrency/stale state;
12. release only with P0=0/P1=0 and a reconstructable final manifest.

The canonical completion protocol is `/PROJECT-COMPLETION-ENGINE`.
The architecture/refactor protocol is `/GRAPH-REFACTOR-V2`.
The multi-agent bootstrap protocol is `/autoprompting`.

---

## 4. PROJECT_DONE CRITICAL PATH

Reconstruct the live DAG, but the last known strategic path was:

```text
Canonical Truth / Event Fabric
        ↓
QA causal integrity
        ↓
Renderer reproducibility
        ↓
Heterogeneous physical master
        ↓
Exact recoverable real creative master
        ↓
Real full-video multimodal provider evidence
        ↓
Creative Tournament
        ↓
Graph-bound Repair Loop
        ↓
Seen + unseen benchmark / primitive qualification
        ↓
CAL2 empirical performance learning
        ↓
Security + recovery + governance
        ↓
Barrier release + serial promotion train
        ↓
Clean-room whole-product gauntlet
        ↓
Immutable release manifest + graph snapshot
        ↓
PROJECT_DONE
```

Never optimize task count. Optimize shortest safe path to terminal invariants.

---

## 5. WHAT WAS ACTUALLY PROVEN

### P3 — heterogeneous renderer master

PR #118 physically proved a deterministic technical composite including HyperFrames, official Lottie execution, semantic alpha, BT.709 normalization and single master audio.

Last known technical master:

- SHA256: `2c11f29609a3f093f10f1ac36c1291e4560bad6fe62e7fc2a1a8c468282735e0`
- 640×360
- 30 fps
- 90 frames
- 3.000 s
- H.264/yuv420p BT.709
- AAC mono 48 kHz
- visible Lottie overlay contribution
- P3 proof ZIP SHA256: `51b460dbdc04a3de8363cf0c11581b86c788015d7d4567ef46a69e5be501f2ce`
- durable Drive mirror: `1oFTvFeY66Or-Cm0aIhaoVcUEtx8bLiv-`

**Boundary:** this is renderer-fabric technical proof, not the canonical creative master and not product-quality authority.

### P4 — exact creative-master recovery

Historical archaeology established:

- RC09E was historically selected as a working-master candidate.
- exact RC09E media artifact/hash was not recoverable.
- never infer RC09E identity from filename, dimensions, narrative, screenshots or a re-render.

The last exactly recoverable creative master is RC06:

- artifact registry id: `ART-44C38A3E73BB`
- Drive file id: `1r3y35J735DhfyW4e5rDBcTRTdEKPPnrf`
- SHA256: `fbfd4fda97b1e4d07b8477018324210324c8115c0f5b9a3515eb27ae255f0f1f`
- bytes: `11,253,549`
- 1080×1920
- 30 fps
- 300 frames
- 10.000 s
- H.264 + AAC 48 kHz stereo

Persisted evidence:

- `evidence/p4_rc06_real_master_recovery_2026-08-31.json`
- `state/p4_real_master_authority.json`
- `src/qa/master_recovery.py`
- `tests/test_master_recovery.py`

Authority reached:

`RECOVERED_EXACT / EXACT_IDENTITY_VERIFIED`

but **not provider-qualified / not release authority / not promoted**.

### P4 provider job

A deterministic provider handoff was prepared:

- job id: `p4-rc06-full-video-provider-job-v1`
- manifest: `evidence/p4_rc06_provider_job_2026-08-31.json`
- exact RC06 SHA bound
- deterministic 24-frame boundary+uniform sampled evidence set
- temporal defect response contract
- 15-dimension creative score contract
- full-video attestation required
- provider identity + provider run id required

No real full-video provider run had been executed.

---

## 6. HIGHEST-PRIORITY NEW P1 — MUST CLOSE BEFORE REAL PROVIDER EXECUTION

The latest independent review on PR #119 found a material P1 authority gap.

Current imported `FullVideoEvidence.authoritative_provider_evidence` / `critique_from_provider_payload()` semantics may allow authority from:

- locally supplied `provider_attested_full_video=True`;
- provider string;
- provider run id;

without requiring the externally returned provider attestation/payload to name and bind the **exact `media_sha256` it actually reviewed**.

This means the next real-provider boundary is not yet strict enough.

### REQUIRED INVARIANT

Before a real provider can raise authority, the external/provider-adapter evidence must explicitly contain and bind:

```text
provider
provider_run_id
exact media_sha256
full_video_scope = true
provider-produced attestation/evidence identity
```

and all values must exactly match the locally prepared evidence object.

A caller-set boolean can never independently promote authority.

Also harden scalar authority inputs against Python coercion:

- literal booleans only where boolean is required;
- no truthy strings;
- no bool-as-int ambiguity;
- no NaN/inf numeric authority;
- bounded strings/IDs;
- provider payload treated as UNTRUSTED_DATA.

### FIRST EXECUTABLE WAVE

If no other agent already owns/fixed this scope:

1. create/claim an isolated P4 provider-attestation-binding workstream stacked on current #119;
2. update `src/qa/temporal_multimodal.py` and only the minimum related contracts;
3. require provider payload/adapter attestation to carry exact media SHA;
4. compare it to `FullVideoEvidence.media_sha256`;
5. make mismatch/missing value fail closed;
6. ensure a locally constructed boolean cannot create authoritative provider evidence;
7. add regression/property/adversarial tests for:
   - wrong media SHA;
   - omitted media SHA;
   - local boolean spoof;
   - wrong provider;
   - wrong run id;
   - truthy-string boolean;
   - NaN/inf score;
   - same run id cross-media replay;
   - provider payload replay on a different candidate;
8. run local-first tests;
9. run exact-head clean-runner `MERGE_SAFE`;
10. code/security review;
11. persist checkpoint to #39/#93/#119;
12. only then consider real provider execution.

Do not weaken tests merely to obtain green CI.

---

## 7. REAL PROVIDER EXECUTION POLICY

After the P1 above is closed:

1. search currently installed connectors/plugins/tools for a genuine full-video multimodal provider;
2. do not assume the provider set from the old session is still current;
3. provider must be able to inspect the entire exact media object and return stable provider/run identity;
4. bind result to exact media SHA;
5. persist raw/normalized provider evidence without secrets/PII;
6. run temporal contract;
7. run creative contract;
8. if provider cannot meet the evidence contract, it is not an authoritative provider for this gate.

If no qualified provider exists:

`P4.REAL_PROVIDER_RUN = BLOCKED_EXTERNAL`

Do not invent a run id. Do not let ChatGPT self-review masquerade as the external provider attestation. Sampled frames/contact sheets may be used for debugging/product insight but authority must remain `NONE` for the real-provider gate.

Then continue every independent non-blocked path instead of idling.

---

## 8. INDEPENDENT FRONTIERS IF P4 PROVIDER REMAINS BLOCKED

Re-read live task DAG first. Historically executable parallel work included:

- P7 security critical path — PR #70 and adjacent trust-boundary PRs;
- cold recovery + agent-death drill through Event Fabric;
- performance learning authority — PR #77;
- primitive/benchmark evidence infrastructure — PR #67/#75;
- governance/direct-main/ruleset protections — including #102/#103 and external GitHub ruleset blocker;
- canonical truth / Event Fabric promotion preparation while respecting #48;
- whole-project complexity/stale-state cleanup that does not depend on empirical provider evidence.

Do not start duplicate work if another active agent owns the same semantic root cause.

---

## 9. DURABLE LEARNINGS FROM THIS CHAT — TREAT AS INVARIANTS

### Authority / coordination

1. **Live truth before reasoning.** Old chat summaries and old PR descriptions are never current authority.
2. **File-disjoint != conflict-free.** Detect semantic collisions through `adr:`, `contract:`, `architecture:`, `root-cause:`, `authority:` scopes.
3. **CI green != merge authority** when `main` or Event Fabric watermark advanced afterward.
4. Before irreversible actions: reread latest Event Bus watermark + live GitHub + recompute candidate.
5. Todoist, Drive docs and graph projections are operator/recovery views, not reverse-write authority.
6. Session must be first-class and zero-context resumable.

### Media / renderer

7. `frame_count / fps` is visual-duration authority; container/mux duration is separate.
8. Browser-originated source does not imply RGB-encoded media. Probe pixel/color family before conversion.
9. Alpha-plane presence does not prove semantic transparency. Test compositing pixels physically.
10. Matching resolution/fps/frames does not prove provenance. Bind compiler/source/runtime/media hashes.
11. One final master audio authority; renderer-local audio must never leak through implicit stream selection.
12. Reproducible Node runtime requires lockfile + `npm ci`, not floating `npm install`.
13. Technical renderer proof is not creative-product proof.

### Evidence / provider

14. Selected historical master != recoverable identity.
15. Exact SHA beats filenames, dimensions, screenshots and narrative matching.
16. Provider timeout after possible acceptance => reconcile before retry.
17. Provider payloads are UNTRUSTED_DATA.
18. Real provider authority requires provider identity + stable run id + exact media SHA + full-video attestation.
19. No caller-set boolean may self-create real-provider authority.
20. Same provider run cannot be replayed across different media/candidates.

### Failure handling

21. Every escaped bug becomes: ROOT_CAUSE → INVARIANT → REGRESSION TEST → ADJACENT FAILURE FAMILY.
22. `SKIPPED`, `CANCELLED`, `NOT_RUN`, `PASS`, `FAIL` remain distinct.
23. Unknown/unsupported media/input/state fails closed; never guess silently.
24. Historical facts are preserved with `SUPERSEDED`, not rewritten.
25. Do not introduce Postgres/Redis/Kafka/K8s/vector DB/CDN/queues without a measured trigger.

### Product

26. Mechanical correctness cannot substitute for creative quality.
27. Creative quality requires actual media, real evidence, 15-dimension scoring, hard thresholds, defects, repair, rerender and rescore.
28. Aggregate benchmark counts never grant per-ID authority.
29. Correlation/performance data cannot self-promote to causal learning rule.
30. `PROJECT_DONE` requires whole-product evidence, not a collection of green component PRs.

---

## 10. PROMOTION BARRIER

Issue #48 was still OPEN at handoff.

Until live authority explicitly releases the barrier:

- keep dependent PRs draft/unmerged;
- no implicit release from CI green;
- no implicit release from score;
- no direct-main promotion;
- no auto-merge;
- no production authority claim.

When barrier release becomes possible, serial promotion is mandatory:

```text
reconcile live truth
→ invalidate stale evidence
→ combined-head proof
→ merge one bounded PR
→ verify main
→ emit canonical event
→ recompute
→ next PR
```

Never bulk-merge the train.

---

## 11. OPERATING LOOP

Use this loop continuously:

```text
RECONSTRUCT LIVE TRUTH
↓
CREATE UNIQUE SESSION
↓
READ BUS + ACTIVE CLAIMS + OPEN PRS
↓
RECOMPILE COMPLETION DAG
↓
SELECT HIGHEST-VALUE SAFE NODE
↓
CLAIM
↓
IMPLEMENT
↓
LOCAL TEST
↓
ADVERSARIAL / SECURITY REVIEW
↓
PHYSICAL / EMPIRICAL PROOF WHERE APPLICABLE
↓
PERSIST EVIDENCE
↓
UPDATE TASK DAG / TODOIST PROJECTION
↓
CHECKPOINT / HANDOFF
↓
RECONCILE LIVE TRUTH AGAIN
↓
CONTINUE
```

Maximum 3 materially identical repair attempts. Then change strategy or declare `STUCK_LOOP`.

---

## 12. REQUIRED HANDOFF STATE

Before the successor agent stops, persist:

- exact `main` SHA;
- branch/head/PR;
- latest Event Fabric watermark or latest relevant #39 checkpoint;
- current phase/wave/milestone;
- exact task completed;
- tests and exact outcomes;
- current authority level;
- unresolved P0/P1;
- blockers/external dependencies;
- artifact/provider/run/media hashes;
- next safe action;
- zero-context continuation instructions.

If the next agent needs this chat transcript, continuity has failed.

---

## 13. SUCCESSOR START COMMAND

Execute now:

```text
/autoprompting
/PROJECT-COMPLETION-ENGINE
/GRAPH-REFACTOR-V2

Read coordination/NEXT_ITERATION_METAPROMPT_2026-09-11.md as historical acceleration context only.
VERIFY LIVE TRUTH BEFORE EXECUTION.

Reconstruct current main, #39, #48, #93, #118, #119 and all active semantic claims.
Start by validating whether the PR #119 P1 provider-attestation/media-SHA binding gap still exists.
If it exists and is unowned, close it first with fail-closed tests and exact-head qualification.
Then search the current connector/plugin/tool graph for a qualified full-video multimodal provider and execute `p4-rc06-full-video-provider-job-v1` only if the provider can produce exact media-SHA-bound full-video evidence with a stable provider_run_id.
If no provider exists, persist BLOCKED_EXTERNAL and immediately continue the highest-value independent READY tasks from the live completion DAG.

Do not stop at planning. Execute, test, attack, persist, recompute, continue.
```
