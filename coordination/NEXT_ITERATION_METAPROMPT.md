/autoprompt

You are the next autonomous MOTION.OS execution session.

This is the personalized continuation packet seeded after pre-production qualification of the self-driving execution loop. It is an acceleration hint, NOT authority.

PREVIOUS_MAIN_SHA: a8d7dbddaeb4ad9779df9883d3cf4e4f6ea4f29d
PREVIOUS_EVENT_WATERMARK: evt_chatgpt_autoloop_verified_20260828T1426+0200
AUTLOOP_PR: #68
AUTLOOP_VERIFIED_HEAD_BEFORE_THIS_DOC: 0046279fc704b1d1431d427b0b0e3606bba94994
ISSUE_48_STATE_AT_COMPILE: OPEN

MANDATORY FRESHNESS GATE
1. Read live main SHA, open/closed/merged PRs and clean-runner conclusions.
2. Read latest Issue #39 Event Fabric watermark and active WRITE/EXCLUSIVE_WRITE claims.
3. Read Issue #48 while open plus AGENTS.md, GOAL.md, STATE.md, TASKS.md and HANDOFF.md.
4. Compare live main/watermark/claims/PR lifecycle to this packet.
5. If any material drift exists, invalidate this packet and recompute with `scripts/next_wave.py` from the live state.
6. Create a new unique session_id and emit WORK_STARTED before mutation.

COMPILED NEXT WAVE — EMPIRICAL PRIMITIVE QUALIFICATION
If live truth has not invalidated this plan, continue the existing primitive-production qualification workstream in PR #67 rather than opening a parallel registry/ledger.

Goal: convert the verified 45×3 qualification plane from contract-only evidence toward the first truthful physical per-primitive evidence batch.

Required execution order:
- read PR #67 latest head, tests, Bus #39 handoff and active scopes;
- verify its scopes are released or explicitly reclaim only non-conflicting primitive-qualification scopes;
- identify the smallest high-signal primitive family for which a production renderer path and independent measurable identity verifier already exist or can be implemented without self-attestation;
- implement primitive-specific observation/signature verification using the existing extraction stack;
- execute a small real physical batch before scaling horizontally;
- require artifact SHA, fixture SHA, frame_count/fps visual duration authority and primitive-identity evidence;
- failed/no-identity cases remain QUARANTINED;
- never promote the historical aggregate `15 verified / 30 quarantined` to per-ID authority;
- use `/gauntlet-loop`: independent verifier, maximum 3 materially distinct repair attempts per invariant, STUCK_LOOP/BLOCKED if no measurable progress;
- run local-first tests, code review, security review and exact-head clean-runner when warranted;
- do not merge while #39/#48 promotion barrier remains active.

Definition of Done for this wake:
- at least one physical primitive qualification case has truthful ID-bound evidence OR a machine-readable blocker proves why no case is safely executable;
- all escaped bugs found become invariant + regression test + adjacent failure family;
- exact tests and authority state are persisted;
- scopes are released;
- live truth is reconstructed again;
- `coordination/NEXT_ITERATION_METAPROMPT.md` is replaced with the newly personalized following wave;
- update the recurring `MOTION OS Autoloop` automation prompt to that newly generated metaprompt after locating it privately by title; keep the hourly schedule unchanged.

Never blindly chain this packet if live truth differs.
