# 2026-09-16 — User directive: learn + PR convergence

Classification: **REFINEMENT**

Source directive, preserved for phase-learning traceability:

> /aprende actualiza agents.md memory.md skills y quizas lo que debamos hacer es estudiar un plan de acción para pasar todos los PR a main d manera cuidadosa ejecutando /codereviews /securityreview y /QAreview

## Context that triggered the directive

A professional-editing recovery demonstrated that significant MOTION.OS capability was present in open PR heads rather than promoted `main`. Treating `main` as the entire capability surface caused an incorrect conclusion about what the system could already do and led to ad-hoc editing outside the recovered reference-conditioned architecture.

## Durable interpretation

- Update agent operating law so capability archaeology inspects live PR frontier before reimplementation.
- Persist the learning in existing canonical memory surfaces rather than silently creating duplicate state/memory authorities.
- Treat `src/skills/registry.py` + `src/skills/runtime.py` and their verified candidate repairs as executable skill authority.
- Build a careful promotion graph for all open PRs before any merge.
- Do not merge every PR independently; resolve ancestry, overlap, repairs, supersession and unique valid deltas.
- Require exact-head `/codereview`, `/securityreview`, `/QAreview`, local/full CI and post-merge verification for each promotion candidate.
- Preserve Issue #48 and native branch protection as hard promotion gates.

This source is evidence. Canonical behavior is compiled into `AGENTS.md`, `docs/PR_CONVERGENCE_MASTERPLAN_2026-09-16.md` and `knowledge/interaction_ledger.jsonl` on the learning branch.
