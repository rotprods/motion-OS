# User Source — Video Reverse Engineering + Replicable Editing Templates

> Captured: 2026-08-28
> Source: direct user instruction in ChatGPT session
> Classification: **REFINEMENT + ADDITIVE** to Phase04 Visual DNA extraction. It does not deprecate the existing deterministic extraction pipeline.
> Preservation rule: source wording below is retained verbatim before any normalization into plans/contracts.

## Raw user directive

> @GitHub entra al event bus de MOTION OS y configura absolutamente todo lo necesario a nivel agentico de su session is context etc y todo lo necesario para entrar a trabajar tb con los demás agentes, tu propuesta y tarea ahora es configurar un sistema de ingeniería inversa sobre vídeos que te voy a mandar para extraer exactamente el estilo de edición frame a frame motion y demás y configurar diferentes plantillas de edición replicables por el sistema

## Normalized intent — non-authoritative interpretation

The requested capability is not a second generic video analyzer. It extends the existing Phase04 measurement chain so a supplied reference can become:

1. evidence-bound frame/timeline observations;
2. a faithful description of editing, motion, typography, layout, camera, transition and sound behavior;
3. a reusable structural template that separates source-specific content from transferable editing logic;
4. an exact-reconstruction contract when fidelity, rather than generalization, is the objective;
5. a style-transfer contract when only the higher-level motion/editing grammar should persist;
6. reusable outputs consumable by MOTION.OS renderers, graph reasoning, prompt generation and future agents without dependence on chat history.

## Hard preservation constraints derived from existing canon

- Measurable facts remain deterministic/low-level whenever a provider exists.
- LLM inference may classify or normalize evidence but must not fabricate frame facts.
- `RECONSTRUCT_EXACT`, `STRUCTURAL_TEMPLATE`, and `STYLE_TRANSFER` are distinct optimization modes.
- Source assets/copy are not silently embedded into generalizable templates.
- Every extracted invariant carries evidence/provenance or an explicit inference/assumption state.
- Frame-accurate does not mean inventing observations for frames that were not actually measured; unknown/unavailable is valid evidence state.
- Nothing moves without a functional or source-evidenced reason.
