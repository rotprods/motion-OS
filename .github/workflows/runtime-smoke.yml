name: Runtime Smoke
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Prove runner startup
        run: |
          echo "runner-started"
          uname -a
          python --version
          node --version || true
          ffmpeg -version | head -1 || true
