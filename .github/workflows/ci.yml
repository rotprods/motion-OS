name: CI (Legacy Manual)
on:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: legacy-ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: pip install -e '.[dev]'
      - run: python scripts/local_verify.py quick --json-out .artifacts/legacy-ci.json
