# AGENTS.md — MOTION.OS

North Star: brief → professional motion master.

Rules:
1. Read `GOAL.md`, `STATE.md`, `HANDOFF.md`, `TASKS.md`, `state/project_state.json` and `config/alignment_weights.json` before work.
2. Distinguish PROPOSED / IMPLEMENTED / EXECUTED / VERIFIED.
3. Never call fixture QA semantic QA.
4. Never promote architecture work unless it removes a current bottleneck or improves measured creative output.
5. Preserve asset/artifact provenance.
6. Run tests + reconciliation + Gauntlet before commit.
7. Production release requires authoritative full-video semantic evidence.
8. GitHub = software truth; Drive = artifacts/progress; SQLite = structured knowledge; Graph = lineage.
9. Local sandbox is disposable.
