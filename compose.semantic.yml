services:
  qdrant:
    image: qdrant/qdrant:v1.19.0
    container_name: motion-semantic-qdrant
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - motion_semantic_qdrant:/qdrant/storage

volumes:
  motion_semantic_qdrant:
